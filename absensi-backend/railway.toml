[build]
builder = "NIXPACKS"

[build.nixpacks]
nixPkgsArchive = "refs/tags/24.11"

[deploy]
startCommand = "node src/server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
